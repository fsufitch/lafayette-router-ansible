# Credentials table entry for submission auth.
# Format: <label> <username>:<password-or-hash>
# Use a hashed password (e.g., from `smtpctl encrypt <password>`) via opensmtpd_printer_password_hash when possible.
{{ opensmtpd_printer_username }} {{ opensmtpd_printer_username }}:{{ (opensmtpd_printer_password_hash | length > 0) | ternary(opensmtpd_printer_password_hash, opensmtpd_printer_password) }}
